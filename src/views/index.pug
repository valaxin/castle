extends layout

block content
  main
    h1.title= title
    if posts
      ol.posts
        each post in posts
          li
            a(href= post.data.slug)= post.data.title
  include components/footer