extends layout


block content
  - var returnbtn = true
  include components/navbar
  br
  main
    section.section
      article
        img.hero(src= context.frontmatter.thumbnail alt= context.frontmatter.thumbnailAlt)
        h1.title= context.frontmatter.title
        .content
          != context.html

    include components/footer